module.exports = {
    content: [
        './index.html',
        './src/**/*.{js,jsx,ts,tsx}'
    ],
    darkMode: 'media', // respect user's OS preference, similar to previous setup
    theme: {
        extend: {},
    },
    plugins: [],
}
